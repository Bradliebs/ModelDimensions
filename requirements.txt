# Core numerics
torch>=2.0
numpy>=1.24
scipy>=1.10

# Encoders / language models (used by concept_cells.encoders and cc_service.encoder)
sentence-transformers>=2.2
transformers>=4.30

# V1 grounded-answer pipeline (Phi-3-mini-4k-instruct generator)
accelerate>=0.27
bitsandbytes>=0.43; sys_platform == "win32"
bitsandbytes>=0.43; sys_platform == "linux"
sentencepiece>=0.1.99

# Datasets (load_wikipedia, retro training prep)
datasets>=2.10

# Plotting (concept-cell experiments)
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65

# SLM controller layer (typed, validated decision schemas)
pydantic>=2.0

# Hybrid retrieval cascade (Phase 1 of the V1 upgrade)
rank-bm25>=0.2.2

# cc_service runtime (FastAPI bank service + CLI clients)
fastapi>=0.104
uvicorn>=0.24
httpx>=0.25
typer>=0.9
rich>=13.0

# Dev
pytest>=7.0
