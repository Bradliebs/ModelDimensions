# Core numerics
torch>=2.0
numpy>=1.24
scipy>=1.10

# Encoders / language models (used by concept_cells.encoders and cc_service.encoder)
sentence-transformers>=2.2
transformers>=4.30

# Datasets (load_wikipedia, retro training prep)
datasets>=2.10

# Plotting (concept-cell experiments)
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65

# SLM controller layer (typed, validated decision schemas)
pydantic>=2.0

# cc_service runtime (FastAPI bank service + CLI clients)
fastapi>=0.104
uvicorn>=0.24
httpx>=0.25
typer>=0.9
rich>=13.0

# Dev
pytest>=7.0
