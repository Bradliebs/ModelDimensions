# Core
torch>=2.0
numpy>=1.24
scipy>=1.10

# Encoders
sentence-transformers>=2.2
transformers>=4.30

# Data + plots
datasets>=2.10
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65

# SLM controller layer (v0.8): typed, validated decision schemas
pydantic>=2.0

# Workbench UI (v1.1, optional): the CLI works without this
streamlit>=1.30

# Curated knowledge packs (v1.8): YAML pack specs (JSON specs need no extra dep)
pyyaml>=6.0

# Document ingest (optional): Word/Excel text for the `ingest <path>` command.
# .txt/.md/.pdf need no extra dep; these add .docx and .xlsx support.
python-docx>=1.1
openpyxl>=3.1

# Dev
pytest>=7.0
