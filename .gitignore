# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environment
.venv/

# Distribution archives
*.tar.gz

# Model / HF caches
.cache/
hf_cache/
.hf_home/

# Concept-cell SQLite banks (the live 14 GB bank lives outside the repo;
# any local rebuilds must not be committed)
bank.db
bank.db-shm
bank.db-wal

# Bank Management Workspace local operator state (overlay edits + Q&A log)
results/v1_bank/overlay.db
results/v1_bank/overlay.db-shm
results/v1_bank/overlay.db-wal
results/v1_bank/qa_history.jsonl

# RetroGPT training checkpoints and run artefacts (large binary state)
out-retro-*/
src/retro/data/**/*.bin
src/retro/data/**/*.npy
src/retro/data/**/*.pt
src/retro/data/**/*.pkl

# Loader / training logs and scratch
*.log
*.err
prep_log.txt
stage3_*.log
ds_sub*.json

# Office lockfiles / paper-edit scratch
~$*
_docx_dump*.txt
_apply_paper_edits.py
_fix_paper_edits.py
